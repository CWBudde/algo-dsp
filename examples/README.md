# Examples

Planned examples:

- `filter_response`
- `thd_analyzer`
- `log_sweep_ir`

Available examples:

- `mixedphase`: compare iterative factorisation, direct phase interpolation,
  complex minimax design, and direct low-group-delay optimisation
  under equal FIR length and delay budgets.
- `graphiceq`: compare the hybrid IIR/FIR octave equaliser with an all-FIR
  design forced to the same latency.
